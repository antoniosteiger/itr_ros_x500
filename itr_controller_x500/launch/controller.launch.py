# TODO:
# -l flag for logging with ros2 bag record (use ./config/essential_topics.txt )
# -c flag for choosing specific controller
# Use pytrees to visualize state machine
# launch state machine and controller (from src?)
# use simulation /clock